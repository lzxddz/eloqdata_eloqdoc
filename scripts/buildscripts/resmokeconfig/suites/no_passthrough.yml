test_kind: js_test

selector:
  roots:
  - tests/jstests/noPassthrough/*.js

  # Self-tests for the Concurrency testing framework are run as part of this test suite.
  - tests/jstests/concurrency/*.js
  exclude_files:
  # Disable inmem_full as per SERVER-27014
  - tests/jstests/noPassthrough/inmem_full.js

# noPassthrough tests start their own mongod's.
executor:
  archive:
    tests:
      - tests/jstests/noPassthrough/backup*.js
      - tests/jstests/noPassthrough/wt_unclean_shutdown.js
  config:
    shell_options:
      nodb: ''
      readMode: commands
