test_kind: js_test

selector:
  roots:
  - tests/jstests/aggregation/**/*.js
  exclude_files:
  - tests/jstests/aggregation/extras/*.js
  - tests/jstests/aggregation/data/*.js
  # The following tests start their own mongod.
  - tests/jstests/aggregation/bugs/cursor_timeout.js
  - tests/jstests/aggregation/bugs/lookup_unwind_getmore.js
  - tests/jstests/aggregation/bugs/lookup_unwind_killcursor.js
  # TODO: Remove when SERVER-23229 is fixed.
  - tests/jstests/aggregation/bugs/groupMissing.js
  exclude_with_any_tags:
  # Tests tagged with the following will fail because they assume collections are not sharded.
  - assumes_no_implicit_collection_creation_after_drop
  - assumes_no_implicit_index_creation
  - assumes_unsharded_collection
  - cannot_create_unique_index_when_using_hashed_shard_key
  # The following tests start their own ShardingTest or ReplSetTest, respectively.
  - requires_sharding
  - requires_replication

executor:
  archive:
    hooks:
      - CheckReplDBHash
      - ValidateCollections
  config:
    shell_options:
      # Use the overrides to implicitly shard all accessed collections, and to error if a test
      # starts its own test fixture - since such a test should not be part of this passthrough.
      eval: >-
        load("tests/jstests/libs/override_methods/implicitly_shard_accessed_collections.js");
        load("tests/jstests/libs/override_methods/detect_spawning_own_mongod.js");
  hooks:
  - class: CheckReplDBHash
  - class: ValidateCollections
  - class: CleanEveryN
    n: 20
  fixture:
    class: ShardedClusterFixture
    num_shards: 1
    mongos_options:
      set_parameters:
        enableTestCommands: 1
    mongod_options:
      nopreallocj: ''
      set_parameters:
        enableTestCommands: 1
        numInitialSyncAttempts: 1
