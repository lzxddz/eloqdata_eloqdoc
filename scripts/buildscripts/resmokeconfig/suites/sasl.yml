test_kind: js_test

selector:
  roots:
  - src/mongo/db/modules/*/tests/jstests/sasl/*.js

# sasl tests start their own mongod's.
executor:
  config:
    shell_options:
      nodb: ''
      readMode: commands
